# Anthropic — claude-haiku-4-5, claude-sonnet-5, claude-opus-4-8
ANTHROPIC_API_KEY=your-anthropic-key-here

# OpenAI — gpt-5.6-luna, gpt-5.6-terra (reasoning-effort sweep)
OPENAI_API_KEY=your-openai-key-here

# Mistral — mistral-small-latest (reached via the OpenAI-compatible endpoint)
MISTRAL_API_KEY=your-mistral-key-here

# Optional: a local Ollama model as a floor probe (no key needed; enable in run.py MODELS).
# Requires `ollama serve` running with the model pulled.
